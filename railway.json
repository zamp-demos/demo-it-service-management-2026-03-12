{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "node interaction-server.cjs",
    "restartPolicyType": "ON_FAILURE"
  }
}